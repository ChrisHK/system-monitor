# Server Configuration
PORT=80
HOST=erp.zerounique.com
NODE_ENV=production
SSL_ENABLED=true

# Production Database Configuration
DB_USER=zerouniq_admin
DB_HOST=127.0.0.200
DB_NAME=zerouniq_db
DB_PASSWORD=is-Admin
DB_PORT=5432
DB_SSL=true

# CORS Configuration
CORS_ORIGIN=https://erp.zerounique.com,http://erp.zerounique.com,https://api.zerounique.com

# WebSocket Configuration
WS_PATH=/ws
WS_PING_INTERVAL=30000

# JWT Configuration
JWT_SECRET=zerounique_erp_jwt_secret_2024_production
JWT_EXPIRES_IN=24h

# Passenger Configuration
PASSENGER_APP_ENV=production 